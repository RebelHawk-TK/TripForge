rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // PDF exports: only the owning user can read/write
    match /users/{uid}/exports/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
